# Tailscale Token Manager Configuration
# Copy this file to .env and fill in your values

# Required: OAuth credentials from Tailscale Admin Console
# Get these from: https://login.tailscale.com/admin/settings/oauth
TAILSCALE_CLIENT_ID=your_client_id_here
TAILSCALE_CLIENT_SECRET=your_client_secret_here

# Optional: Timezone for logging (default: UTC)
TZ=America/New_York

# Optional: Port for the proxy server (default: 1180)
# PROXY_PORT=1180
